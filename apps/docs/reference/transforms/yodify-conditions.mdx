---
title: "Yodify conditions"
description: "Moves a safe literal-shaped right operand to the left of a comparison."
keywords: ["yodifyConditions", "JavaScript transform"]
---
{/* Generated by scripts/generate-transforms.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.yodifyConditions</Badge>

## Configuration

Use `transforms.yodifyConditions` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    yodifyConditions: true,
  },
})
```

## What it changes

Moves a safe literal-shaped right operand to the left of a comparison.
Relational operators are flipped after swapping.

Unary operations over literals also count when their coercion cannot act.

## Before and after

<CodeGroup>

```js title="Before" lines
if (x === 1) {}
if (name !== "foo") {}
if (count > 5) {}
if (idx <= 10) {}
```

```js title="After" lines
if (1 === x) {}
if ("foo" !== name) {}
if (5 < count) {}
if (10 >= idx) {}
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/yodify-conditions.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/yodify-conditions.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
