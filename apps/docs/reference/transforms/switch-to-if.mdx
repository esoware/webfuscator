---
title: "Switch to if"
description: "Lowers switch to an if chain or, when cases fall through, a labeled block with a fallthrough flag."
keywords: ["switchToIf", "JavaScript transform"]
---
{/* Generated by scripts/generate-transforms.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.switchToIf</Badge>

## Configuration

Use `transforms.switchToIf` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    switchToIf: true,
  },
})
```

## What it changes

Lowers `switch` to an `if` chain or, when cases fall through, a labeled block
with a fallthrough flag. Case tests keep source order and evaluation count.
Cases with lexical or function declarations stay unchanged because separate
`if` blocks cannot preserve the shared switch scope.

## Before and after

<CodeGroup>

```js title="Before" lines
switch (x) {
  case 1: a(); break;
  case 2: b();
  case 3: c(); break;
  default: d();
}
```

```js title="After" lines
_switch: {
  var _switchValue = x;
  var _switchFall = false;
  if (_switchFall || _switchValue === 1) { _switchFall = true; a(); break _switch; }
  if (_switchFall || _switchValue === 2) { _switchFall = true; b(); }
  if (_switchFall || _switchValue === 3) { _switchFall = true; c(); break _switch; }
  d();
}
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/switch-to-if.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/switch-to-if.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
