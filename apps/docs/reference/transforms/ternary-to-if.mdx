---
title: "Ternaries to if statements"
description: "Lowers conditional expressions to if statements."
keywords: ["ternaryToIf", "JavaScript transform"]
---
{/* Generated by scripts/generate-transforms.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.ternaryToIf</Badge>

## Configuration

Use `transforms.ternaryToIf` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    ternaryToIf: true,
  },
})
```

## What it changes

Lowers conditional expressions to `if` statements. Expression values use a
temporary. Parameter defaults and repeating loop tests stay unchanged because
hoisting would change when they run.

## Before and after

<CodeGroup>

```js title="Before" lines
var y = a ? b : c;
log(d ? e : f);
```

```js title="After" lines
var y;
if (a) { y = b; } else { y = c; }
var _ternaryResult;
if (d) { _ternaryResult = e; } else { _ternaryResult = f; }
log(_ternaryResult);
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/ternary-to-if.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/ternary-to-if.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
