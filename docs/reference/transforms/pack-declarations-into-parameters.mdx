---
title: "Pack declarations into parameters"
description: "Moves leading literal-valued var declarations into trailing default parameters."
keywords: ["packDeclarationsIntoParameters", "JavaScript transform"]
---
{/* Generated by scripts/generate-transform-docs.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.packDeclarationsIntoParameters</Badge>

## Configuration

Use `transforms.packDeclarationsIntoParameters` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    packDeclarationsIntoParameters: true,
  },
})
```

## What it changes

Moves leading literal-valued `var` declarations into trailing default
parameters. Arrows, rest parameters, name collisions, and functions with
callers that could fill a new slot stay unchanged.

Packing also requires unchanged `arguments`, directive, and hoisting behavior.

## Before and after

<CodeGroup>

```js title="Before" lines
function run() {
  var x = 1;
  return x;
}
```

```js title="After" lines
function run(x = 1) {
  return x;
}
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/pack-declarations-into-parameters.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/pack-declarations-into-parameters.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
