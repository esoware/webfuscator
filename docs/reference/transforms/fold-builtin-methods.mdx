---
title: "Fold built-in methods"
description: "Replaces built-in calls and reads with smaller syntax or host-computed constants."
keywords: ["foldBuiltinMethods", "JavaScript transform"]
---
{/* Generated by scripts/generate-transform-docs.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.foldBuiltinMethods</Badge>

## Configuration

Use `transforms.foldBuiltinMethods` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    foldBuiltinMethods: true,
  },
})
```

## What it changes

Replaces built-in calls and reads with smaller syntax or host-computed
constants. Host evaluation requires known arguments and receivers. Shadowed
names, optional forms, and regexp methods stay unchanged.

## Before and after

<CodeGroup>

```js title="Before" lines
Math.pow(2, 3);
Array.of(1, 2, 3);
"abc".toUpperCase();
Math.PI;
parseInt("0x1F", 16);
```

```js title="After" lines
8;
[1, 2, 3];
"ABC";
3.141592653589793;
31;
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/fold-builtin-methods.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/fold-builtin-methods.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
