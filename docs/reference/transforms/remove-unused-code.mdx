---
title: "Remove unused code"
description: "Removes unused declarations and expression statements that have no effects."
keywords: ["removeUnusedCode", "JavaScript transform"]
---
{/* Generated by scripts/generate-transform-docs.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.removeUnusedCode</Badge>

## Configuration

Use `transforms.removeUnusedCode` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    removeUnusedCode: true,
  },
})
```

## What it changes

Removes unused declarations and expression statements that have no effects.
Sweeps continue because each removal can strand another binding.

## Before and after

<CodeGroup>

```js title="Before" lines
function foo() { return "foo"; }
function bar() { return "bar"; }
var unused = 1;
foo();
```

```js title="After" lines
function foo() { return "foo"; }
foo();
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/remove-unused-code.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/remove-unused-code.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
