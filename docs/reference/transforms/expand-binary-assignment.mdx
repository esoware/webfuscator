---
title: "Expand binary assignments"
description: "Rewrites arithmetic and bitwise compound assignments as LHS = LHS op RHS."
keywords: ["expandBinaryAssignment", "JavaScript transform"]
---
{/* Generated by scripts/generate-transform-docs.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.expandBinaryAssignment</Badge>

## Configuration

Use `transforms.expandBinaryAssignment` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    expandBinaryAssignment: true,
  },
})
```

## What it changes

Rewrites arithmetic and bitwise compound assignments as `LHS = LHS op RHS`.
Logical compound assignments are handled by `expandLogicalAssignment`.

Member objects and computed keys are cached so the read and write use the same
evaluated receiver. The cache assignment stays on the outer write target,
which evaluates before its right side.

Compound and expanded forms use the same operator semantics, including BigInt.

## Before and after

<CodeGroup>

```js title="Before" lines
a += b;
a **= 2;
obj.x -= y;
arr[idx()] |= v;
```

```js title="After" lines
a = a + b;
a = a ** 2;
(_obj = obj).x = _obj.x - y;
(_arr = arr)[_idx = idx()] = _arr[_idx] | v;
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/expand-binary-assignment.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/expand-binary-assignment.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
