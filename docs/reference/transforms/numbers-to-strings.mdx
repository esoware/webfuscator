---
title: "Numbers to strings"
description: "Rewrites numeric literals as strings coerced with unary plus."
keywords: ["numbersToStrings", "JavaScript transform"]
---
{/* Generated by scripts/generate-transform-docs.mjs. Do not edit directly. */}

<Badge color="blue" icon="sliders-horizontal" stroke>Option: transforms.numbersToStrings</Badge>

## Configuration

Use `transforms.numbersToStrings` in the options passed to `obfuscate`:

```js obfuscate.mjs highlight={3}
const output = obfuscate(source, {
  transforms: {
    numbersToStrings: true,
  },
})
```

## What it changes

Rewrites numeric literals as strings coerced with unary plus. Under `+`, `-`,
or `~`, the existing operator supplies that coercion. Other unary operators
keep the wrapper because their string behavior differs.

## Before and after

<CodeGroup>

```js title="Before" lines
var x = arr[42] + 0.5 * 1e3;
var y = -5 * ~3;
```

```js title="After" lines
var x = arr[+"42"] + +"0.5" * +"1000";
var y = -"5" * ~"3";
```

</CodeGroup>

<Columns cols={2}>
  <Card title="View the implementation" icon="code-xml" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/src/transforms/numbers-to-strings.ts" horizontal>
    Open the TypeScript implementation on GitHub.
  </Card>
  <Card title="Read the behavior tests" icon="flask-conical" href="https://github.com/esoware/webfuscator/blob/main/packages/webfuscator/tests/transforms/numbers-to-strings.test.ts" horizontal>
    Review behavior checks and cases this transform leaves alone.
  </Card>
</Columns>
